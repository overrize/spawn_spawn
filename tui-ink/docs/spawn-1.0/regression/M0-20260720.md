# 回归记录 — M0 埋点契约（里程碑出口）

## 基本信息

| 项 | 值 |
|---|---|
| 日期 | 2026-07-20 |
| 触发原因 | 里程碑出口（M0 关闭） |
| 对应里程碑 | M0 |
| WORKPLAN 版本 | v1.0.0 |
| PLAN 版本 | Draft v0.2 |
| 代码基线 | main @ 06534df 之后（含 M0-3 整改 / M0-5 / M0-6 实现，执行侧工作区，尚未单独提交实现代码） |
| prompt_version | 不涉及 |
| 执行人 | Claude (QA) |

## 执行范围与结果

| 检查项 | 命令/方式 | 结果 | 备注 |
|---|---|---|---|
| 类型检查 | `npm run typecheck` | ✅ | 干净 |
| 全量测试 | `npm test` | ✅ 416 passed / 0 failed | 105 suites |
| 事件序列快照 | orchestration-golden（M1-5）| ✅ | 25 用例全绿，编排行为未回归 |
| 里程碑验收项 | 逐条见下 | ✅ | |
| 真实任务出口 | `npx tsx scripts/m0-exit-check.mts` | ✅ PASS | 见下 |

### 逐任务核对（对照「回归验证」列）

| 任务 | 验证标准 | 结果 |
|---|---|---|
| M0-1 | 类型 + 写读 + 损坏行容错单测 | ✅ health-metrics.test.ts |
| M0-2 | 坏输出断言事件产出 | ✅ parse_failed/repaired/fallback 三类断言 |
| M0-3 | ProcessManager 断言 HealthMetric | ✅ tool_call_repeated 断言 + metricTypeForAlert 4 分支单测（no_progress/dispatch_timeout 映射经此覆盖）。hard_circuit_fired 递延 M1-6e |
| M0-4 | pm-hierarchy 扩展断言 | ✅ memory_fact_merged/dropped 断言含 meta。resume_context_missing 递延 M1-6e |
| M0-5 | 完成守卫测试扩展 | ✅ trace_completed/failed 进 store.applyEvent 且 smoke 断言。done-guard 两事件递延 M1-6e |
| M0-6 | 手工验收 + 快照测试 | ✅ formatMetricsReport 快照断言 + 出口脚本渲染验证 |

## HealthMetric 摘要

出口脚本单次真实任务产出（首次基线，无上次对比）：

| event_type | 次数 | 备注 |
|---|---|---|
| protocol_repaired | 1 | JSON 字面量修复 |
| protocol_parse_failed | 1 | 不可恢复 JSON |
| fallback_message_emitted | 1 | 裸文本降级 |
| memory_fact_merged | 1 | 相似 fact 合并 |
| tool_call_repeated | 1 | 同路径 10 次 |
| trace_failed | 1 | worker 失败终态 |
| trace_completed | 1 | TL 成功终态 |

隔离验证：跑 smoke+pm-hierarchy 后真实 `.spawn/metrics` 零增长；node:test 进程写系统临时目录，非 test 进程写 cwd `.spawn/metrics`（出口脚本确认）。

## 失败项与处置

| 失败项 | 现象 | 归因 | 处置 | 关联 |
|---|---|---|---|---|
| M0-3 首轮 | no_progress/dispatch_timeout 无断言 | 映射函数私有 | 退回→导出 metricTypeForAlert + 4 分支单测 | 已通过 |
| M0-6 首轮 | /metrics handler 不可测 + 测试污染真实 store | index.tsx 闭包 + 默认 store 无隔离 | 退回→抽 formatMetricsReport + node:test 写临时目录 | 已通过 |

## 递延项（转 M1-6e 回归验证列）

以下 4 个埋点位于 `index.tsx` 闭包内，结构上不可单测，随 M1-6e glue 拆出后必须补测试：
`hard_circuit_fired` · `resume_context_missing` · `agent_done_blocked_by_guard` · `test_failed_but_claimed_done`

观察项（转 M1）：resume_context_missing 当前仅覆盖 loadMemory 整体缺失，PLAN 契约中"缺 history/todo/child state"部分缺失场景未检测。

## 结论

- [x] 通过 — M0 里程碑关闭
- [ ] 不通过
- [ ] 回滚

附注：M0 交付「感知层」——系统失败/降级/恢复now有结构化数据契约。M1 起进入结构拆分，递延断言在 M1-6e 补齐。执行侧实现代码需落库（QA 只提交文档与基线）。
