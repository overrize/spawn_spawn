# M1-6 Regression — 2026-07-22

Scope:
- Moved the legacy TUI/runtime entry body from `src/index.tsx` into `src/runtime/AgentRuntime.tsx`.
- Added `RuntimeContext` as the shared runtime-state injection point.
- Added runtime facade modules for observability, tools, memory, and orchestration imports.
- Added `AgentIdlePolicy` and converted EV-011/012/013 from deferred specs to executable assertions.
- Kept behavior equivalent; no golden snapshots were changed.

Validation:
- `npm run typecheck` — pass
- `node --import tsx/esm --test src/tests/baseline/orchestration-golden.test.ts` — 25 pass / 0 fail
- `node --import tsx/esm --test src/tests/eval/scenario-eval.test.ts` — 31 pass / 0 fail / 0 todo
- `npm test` — 524 tests / 517 pass / 0 fail / 7 todo
- `npm run test:full` — 65 pass / 0 fail, exit 0

Notes:
- `src/index.tsx` is now a one-line entry import.
- Further extraction can move handlers out of `AgentRuntime.tsx` behind the same runtime module seams without touching the CLI entry again.
