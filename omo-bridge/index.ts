/**
 * omo-bridge — barrel export for the omo-fork integration layer.
 *
 * Usage:
 *   import { spawnBridge, SpawnBridge, AgentInfo } from "fork-agent/omo-bridge";
 */

export { SpawnBridge, spawnBridge } from "./spawn-bridge";
export type { AgentInfo } from "./spawn-bridge";
